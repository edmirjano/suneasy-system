apiVersion: v1
kind: Service
metadata:
  name: dashboard
  namespace: suneasy-dev
  labels:
    app: dashboard
    component: frontend
spec:
  type: ClusterIP
  selector:
    app: dashboard
  ports:
    - protocol: TCP
      port: 4000
      targetPort: 4000
      name: http
