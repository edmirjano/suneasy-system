apiVersion: apps/v1
kind: Deployment
metadata:
  name: client-web
  namespace: suneasy-dev
  labels:
    app: client-web
    component: frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: client-web
  template:
    metadata:
      labels:
        app: client-web
        component: frontend
    spec:
      containers:
        - name: client-web
          image: suneasy/client-web:dev
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 4500
          env:
            - name: API_URL
              valueFrom:
                configMapKeyRef:
                  name: frontend-config
                  key: API_URL
            - name: WS_URL
              valueFrom:
                configMapKeyRef:
                  name: frontend-config
                  key: WS_URL
            - name: NODE_ENV
              value: 'development'
          resources:
            requests:
              memory: '1Gi'
              cpu: '100m'
            limits:
              memory: '4Gi'
              cpu: '1000m'
