apiVersion: v1
kind: Service
metadata:
  name: adminer
  namespace: suneasy-dev
  labels:
    app: adminer
    tier: infrastructure
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: adminer
