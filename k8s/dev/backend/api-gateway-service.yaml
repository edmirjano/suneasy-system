apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: suneasy-dev
  labels:
    app: api-gateway
    component: backend
spec:
  type: ClusterIP
  selector:
    app: api-gateway
  ports:
  - name: http
    protocol: TCP
    port: 8000
    targetPort: 80
